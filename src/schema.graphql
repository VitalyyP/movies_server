type Query {
  movies(page: Int): Movies
}

type Movies {
  page: Int!
  totalResults: Int!
  totalPages: Int!
  results: [Movie]!
}

type Movie {
  id: ID!
  title: String!
  originalTitle: String!
  originalLanguage: String!
  releaseDate(format: String): String!
  posterPath: String
  genres: [Int]
  adult: Boolean
  overview: String
  backdropPath: String
  popularity: Float
  voteCount: Int
  video: Boolean
  voteAverage: Float
}

type Genre {
  id: Int!
  name: String!
}
